%% test_eigenfaces
match = zeros(10);

for ii = 1:10
    for iter = 1:100
        eigenfaces

        if(e(i_m) < 38)
        %if(e(i_m) > 0.46)
            match(ii) = match(ii) + 1;
        end

    end
    
end
